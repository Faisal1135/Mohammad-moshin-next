import type { NextPage } from "next";
import AppBar from "../components/Head/MyNavbar/AppBar";

const Home: NextPage = () => {
  return (
    <div>
      <AppBar />
    </div>
  );
};

export default Home;
